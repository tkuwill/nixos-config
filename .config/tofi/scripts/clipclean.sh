#!/bin/sh
# Description: a script which can delete clipboard history.
cliphist wipe && dunstify -a "changeVolume" -i /home/will/Pictures/sysicon/chat.png -t 4000 "Clipboard history are all deleted !" 
